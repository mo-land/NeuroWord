<%= paginate @questions, param_name: :questions_page, params: { tab: 'user_questions' } %>
<div class="grid sm:grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-12 mx-auto max-w-screen-6xl py-8 items-stretch">
  <%= render 'user_questions' %>
</div>
<%= paginate @questions, param_name: :questions_page, params: { tab: 'user_questions' } %>
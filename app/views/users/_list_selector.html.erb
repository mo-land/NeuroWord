<%= select_tag :list_id,
               options_for_select(lists.map { |list| [list.name, list.id, { "data-is-favorite" => list.is_favorite }] }, selected_list_id),
               class: "select select-bordered w-full max-w-xs",
               id: "list_id",
               data: {
                 list_selector_target: "select",
                 action: "change->list-selector#changeList"
               } %>
